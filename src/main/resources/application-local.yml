hmpps-auth:
  url: https://sign-in-dev.hmpps.service.justice.gov.uk/auth

service:
  probation-integration-delius:
    base-url: https://approved-premises-and-delius-dev.hmpps.service.justice.gov.uk

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true